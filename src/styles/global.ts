import {StyleSheet} from 'react-native';

export const useGlobalStyles = () =>
  StyleSheet.create({
    title: {
      color: '#9B9898',
      fontWeight: '800',
      fontSize: 14,
      marginBottom: 10,
    },
  });
